# WhatsApp Service Configuration
PORT=3000

# Cloudflare Worker API Configuration
WORKER_API_URL=https://your-worker.workers.dev
AGENT_API_KEY=YOUR_SECRET_API_KEY

# Database Configuration (if needed for direct access)
# For now, all data access goes through the Worker API
